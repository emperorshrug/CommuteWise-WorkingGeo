export default function Home() {
  // We return null because the MapCanvas is already rendered
  // in the background (MainLayout Layer 0).
  // We will add "Floating UI" buttons here later (like 'Filter Terminals').
  return null;
}
